services:
  postgres:
    image: postgres:16-alpine
    container_name: hockey-platform-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: hockey_platform
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
